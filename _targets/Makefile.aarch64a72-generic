#
# Makefile for Phoenix-RTOS 3 filesystems
#
# Generic AArch64 A72 filesystems
#
# Copyright 2026 Phoenix Systems
#

# nfs: userspace NFS-client VFS server (#153). Links the libnfs port; only
# built for this networked target (do not add unconditionally — a no-network
# target would ship a dead binary that fails to link without libnfs.a).
DEFAULT_COMPONENTS := dummyfs libext2 libjffs2 nfs
